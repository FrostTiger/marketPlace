shopping-card(ng-repeat="card in $ctrl.owner.cards" card="card" ng-if="!card.isArchived && !$ctrl.setArchiveScreen")
shopping-card(ng-repeat="card in $ctrl.owner.cards" card="card" ng-if="card.isArchived && $ctrl.setArchiveScreen")